# Copy to .env and point at your deployed backend once it's live on Render.
# Leave unset for local dev - vite.config.js proxies /api to localhost:8000.

VITE_API_URL=https://your-backend.onrender.com
